// Iteration
// Iteration is the process of repeating a block of code multiple times based on certain conditions.

// Iteration
var numbers = [1, 2, 3, 4, 5];
numbers.forEach(function(num) {
  console.log(num);
});